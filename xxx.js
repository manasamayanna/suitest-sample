var username = "manasamayanna";


function display_cher_by_char(uname, xpath)
{

    var newstr = '';
    for(i = 0; i < uname.length; i ++)
    {
        console.log(uname.charAt(i))
        xpth_cont = xpath+ charAt(i)
        click 
        newstr += uname.charAt(i)
    }
    return newstr
}

hh = display_cher_by_char(username)
console.log(hh)